[build]
  command = "npm run build"
  publish = ".next"

# Cron de coleta: desabilitado até liberar produção.
# Quando for ativar, recriar netlify/functions/collect-feedbacks-cron.mts
# (schedule horário) e definir CRON_SECRET no Netlify.
